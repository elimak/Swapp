package fr.swapp.input.delegate 
{
	//import flash.display.DisplayObject;
	
	/**
	 * @author ZoulouX
	 */
	public interface IInputDelegate
	{
		/**
		 * Handle or not a target with a specified input type.
		 * @param	pInputType : The input type to handle (mouse or touch)
		 * @param	pTarget : The target to handl
		 * @return : Return the target to handle, return null to cancel action.
		 */
		//function inputHandleTarget (pInputType:uint, pTarget:DisplayObject):DisplayObject
	}
}